<div class="row">
	<div class="col-2">
		<i class="fas fa-award"></i>
	</div>
	<div class="col-10 ps-0">
		{{ form_start(form, {'action': path('new_statistic', {'id': beer_id}), 'method': 'post'}) }}

		<div class="input-group input-group-sm">
			{# <input type="text" class="form-control" aria-label="Dollar amount (with dot and two decimal places)"> #}
			{{ form_widget(form.score, {'attr': {'class': 'form-control bg-white'}}) }}
			<span class="input-group-text">/ 10</span>
			<button class="btn btn-outline-primary" type="submit">Noter</button>
		</div>

		{{ form_end(form) }}
	</div>
</div>
